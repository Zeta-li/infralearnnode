# **GEMM**

文章链接

[How to Optimize a CUDA Matmul Kernel for cuBLAS-like Performance: a Worklog](https://siboehm.com/articles/22/CUDA-MMM)

1.对于原始实现，具体来说，这两个向量的长度都为 K，因此我们需要遍历从 0 到 K-1 的所有索引 k，依次计算 `A[gx][k] × B[k][gy]`，并将这些乘积累加，最终结果写入 `C[gx][gy]`中。（这里线程的组织是和实际矩阵的排布对应的，是二维的）

每个输出元素：需要k-1次加法和k次乘法

问题：全局访存延迟高，计算密度低（计算/访存比（compute-to-memory ratio）极低），缺乏对共享显存的利用



2.优化采用共享显存：每次加载一批数据进入共享显存，计算得到一小块矩阵的结果

（这里线程的组织是1D的，核心原因是 **warp 的访存连续性**，方便让一个 warp 处理多行/多列的 tile）